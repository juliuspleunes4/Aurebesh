# Force all .sql files to be counted as SQL language
*.sql linguist-language=SQL
**/*.sql linguist-language=SQL

# Ensure SQL files are detected and counted
*.sql linguist-detectable=true
**/*.sql linguist-detectable=true
